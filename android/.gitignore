*.iml
.gradle/
/local.properties
.idea/
.DS_Store
build/
/captures
.externalNativeBuild
.cxx
local.properties

# The bundled model IS committed (overrides the repo-root "*.tflite" ignore) so the app builds standalone.
!app/src/main/assets/face_hand.tflite
