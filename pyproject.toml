[project]
name = "hand-keypoints"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "insightface>=1.0.1",
    "ultralytics>=8.4.70",
]
