# Python / uv
.venv/
__pycache__/
*.py[cod]

# Datasets (auto-downloaded by Ultralytics, large)
datasets/

# Golf pipeline data + outputs (raw videos, extracted frames, LS exports — large, regenerable)
data/
out_prelabel/
golf_reviewed/
out_sam/
out_mined/
out_review*/
out_sam_preview/
*.sqlite3
ls_train.json
ls_val.json
ls_test.json

# Training outputs / runs (checkpoints, plots, large)
runs/

# Downloaded pretrained weights / exported models (regenerable)
*.pt
*.onnx
*.tflite

# Benchmark exports + int8 calibration arrays (regenerable)
bench/
*.npy

# Logs
*.log

# OS / editor
.DS_Store
*.swp
landmark_check.png

# downloaded ONNX models for the caption app (auto-fetched)
models/
